import React from 'react';

const HomeContact = () => {
    return (
        <section className="contact" id="contact">
            contact placeholder
        </section>
    );
};

export default HomeContact;